{
	"name": "solar",
	"version": "0.0.1",
	"license": "GPL-3.0-or-later",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"i18n:build": "node scripts/i18n-build.mjs",
		"verify:i18n": "node scripts/verify-i18n.mjs",
		"test": "node scripts/verify-i18n.mjs && npm test -w @ortho-earth/ephem",
		"deploy": "npm run verify:i18n && npm test -w @ortho-earth/ephem && npm run i18n:build && vite build && wrangler deploy",
		"og": "node scripts/og-shot.mjs"
	},
	"dependencies": {
		"@ortho-earth/ephem": "*",
		"geopbf": "*",
		"native-bucket": "*"
	},
	"devDependencies": {
		"vite": "^5.0.0"
	}
}
