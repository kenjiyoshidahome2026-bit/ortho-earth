{
	"name": "ortho-nl",
	"version": "0.0.1",
	"license": "GPL-3.0-or-later",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"deploy": "node ../ortho-japan/scripts/check-i18n-tables.mjs && vite build && rm -rf dist/site/nl/moj-local && find dist/site -name .DS_Store -delete && wrangler deploy",
		"preview:prod": "vite build && wrangler dev"
	},
	"dependencies": {
		"@loaders.gl/3d-tiles": "^4.3.0",
		"@loaders.gl/core": "^4.3.0",
		"@loaders.gl/draco": "^4.3.0",
		"@ortho-earth/japan": "*"
	}
}
