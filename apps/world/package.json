{
	"name": "world",
	"private": true,
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "vite build && find dist -name .DS_Store -delete && wrangler deploy"
	},
	"dependencies": {
		"common": "*",
		"native-bucket": "*"
	},
	"devDependencies": {
		"vite": "^6.0.0"
	}
}
